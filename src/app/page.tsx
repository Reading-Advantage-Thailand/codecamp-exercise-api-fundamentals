// TODO: Import fetchUsers from @/lib/api
// TODO: Import Link from next/link

// TODO: Implement this Server Component
// - Call fetchUsers() (this is a Server Component, so you can await directly)
// - Render each user as a card with: name, email, company name
// - Wrap each card in a Link to /users/[id]

export default async function HomePage() {
  return (
    <div>
      <h2>Users</h2>
      <p>TODO: Fetch users and display them here. Each user should link to their detail page.</p>
    </div>
  );
}
