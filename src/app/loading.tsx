// TODO: Create a loading component
// - Show a "Loading..." message or a spinner
// - This displays automatically while Server Components are loading

export default function Loading() {
  return <p>Loading...</p>;
}
